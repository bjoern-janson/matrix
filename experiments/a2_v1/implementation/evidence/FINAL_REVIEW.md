# Task 1 independent rereview

Reviewed commit `929d384972f9bffd12a8380c8204600d3a393941` in the validation worktree, with HEAD confirmed at that commit and a clean tracked working tree before this report. Read the frozen `experiments/A2_PREREG_V1.md`, machine contract, original review, implementation report and fix appendix, production modules, pinned V0 model, README, and focused tests, including the new regression suite. Inspected the fix diff and theorem table column names.

**Spec-compliance verdict: PASS for independent static rereview.**

**Code-quality verdict: PASS for the reviewed scope.**

No remaining critical or important defect was identified in C1/C2/I1–I6 or in the surrounding changed execution and custody paths. No scientific claim or validation execution result follows from this verdict.

| Finding | Rereview disposition and evidence |
|---|---|
| C1 | Resolved. `checks.check_pair` independently initializes all 48 IDs, follows the complete before/after recurrence, pins priority to the requested unit, derives predictions by inverse lookup in independently enumerated forward maps, checks actual correctness, feedback and each episode's resources, and links final live state and active constructor to that recurrence. Full control live state and evaluation snapshots are checked. The runner no longer replaces recorded priority evidence. |
| C2 | Resolved. `_measure` realizes the pinned original V0 `ConstructorInput`, then calls its policy executor; the original policy's action interface sees observations only. Independent checks compare complete policy declarations, every action/observation/success/cost record, exact eligible witness sets, algebraic frontiers and primary linkage. Both changed-pair transplant directions actually replace the recipient constructor's active field, realize and execute policies, retain measurements, and compare to the donor frontier. |
| I1 | Resolved. Source manifest, complete unit identities, compiled reference rows, and their preparation binding are exclusively serialized and verified before the first paired boundary. Reference tables are generated without V1 learner execution and frozen recursively in memory. |
| I2 | Resolved. Protocol identity is pinned, nested authority/reference structures are immutable, and source manifests are retained. Reverification covers current source digest/manifest, freshly loaded authorities and all accumulated artifact hashes before validation sealing, after the validation receipt and before target entry, and around final output seals. Newly written receipts are included in verification. |
| I3 | Resolved for caught execution/preparation faults. Bookkeeping and failure handling now surround preparation. Entry and return counters are distinct; returned evidence is retained before checks. Engine exceptions carry completed and in-progress arm/policy context. Checker failures retain preceding checks, and diagnostics plus an invalid receipt preserve these records. A late invalidation preserves earlier receipts and explicitly invalidates the attempted run. |
| I4 | Resolved. `_primary` sorts the combined primary rows lexicographically while raw evidence preserves phase execution order. Canonical encoding remains consistent with the frozen contract. |
| I5 | Resolved. Requested/returned unit and stratum are checked at the boundary; preparation verifies exact declared manifests and reference keys. Reference and final coverage compare exact sets with uniqueness, universe and stratum checks; both verify per-world control baselines. Exact entered/returned phase counters are checked at each completed gate. |
| I6 | Resolved as a test-coverage finding. The new tests meaningfully exercise trajectory and execution corruption, preparation timing, retained failures, source/seal changes, production reference matching at real checked reference failure cells, fresh science phase order with a synthetic target sentinel, primary sorting and exact coverage. Derived tests explicitly assert effect-modification membership/witness and encounter any/common loss families, including the empty-common case. The report corrects the omitted reference smoke identity. Tests were read, not rerun by this reviewer. |

The unchanged derived implementation was also inspected against the frozen definitions: noncontrol domain, oriented set contrasts, deterministic first witnesses, and separate any/common loss structures remain consistent. The science path retains the mandatory fresh reference gate and completes coverage/invariants before primary admission; derived output is computed after primary sealing and binds that primary hash.

No tests, assay runs, reference smoke executions, prospective calls, source edits, index operations, or Git ref operations were performed by this reviewer. This review file is the only file written. The implementer's reported 30-test result has not been independently reproduced here. Complete-gate runtime, resource use and the actual 2,176-reference validation receipt remain controller-owned verification work. This PASS supports proceeding to the already authorized validation-only gate on unchanged reviewed code; it does not authorize prospective execution.
