# A2-V1 current program state

**Standing:** COMPLETE / SCIENTIFIC INTERPRETATION ADMITTED.  
**Canonical result:** [`../A2_V1_RESULT.md`](../A2_V1_RESULT.md).  
**Current machine status:** [`STATUS.json`](STATUS.json).  
**Execution custody:** [`EXECUTION_CUSTODY.md`](EXECUTION_CUSTODY.md).

This file is a current-state index written after the admitted A2-V1 execution. It does not amend the frozen preregistration or retroactively change the standing of any historical audit, implementation or validation artifact.

## Earned by A2-V1

On the preregistered noncontrol reporting domain of 40 hidden worlds, 24 conjugation-generated identity-first active-survivor priorities and eight cyclic encounter rotations:

- encounter order changes the exact feedback-frontier contrast for **912 / 960** fixed `(h,P)` slices;
- active-survivor priority changes the exact feedback-frontier contrast for **270 / 320** fixed `(h,k)` slices;
- priority modifies the consequence of encounter order in **40 / 40** noncontrol worlds;
- `L_any_P = 0` and `L_common_P = 0`;
- `L_any_K = 0` and `L_common_K = 0`.

The finite localization earned by this assay is:

```text
fixed frontier semantics
        +
active-survivor priority
        ×
cyclic encounter order
        ↓
exact corrective-frontier contrast
```

Within the frozen family, A2-V0's loss/tradeoff structure is therefore not correctly localized to encounter order alone or to the original lexicographic priority alone. It is contingent on the joint priority × encounter trajectory.

The priority × encounter result is **effect modification**, not a unique additive decomposition and not a percentage attribution between the two factors.

## Preserved negative boundaries

A2-V1 does **not** establish:

- results for arbitrary identity-first rankings;
- results for all `8!` encounter orders;
- results for arbitrary search policies, active experiment choice or other learners;
- a general law of non-monotonic feedback or adaptation;
- general corrigibility or general self-improvement;
- a unique mediator among representation, diagnostic repertoire, policy machinery or other learned state;
- `MATRIX_1`.

The constructor transplantation checks remain contract checks, not independent mediation evidence.

## Historical stages remain historical

The following records intentionally describe earlier epistemic states and are not rewritten to sound current:

- [`CUSTODY.md`](CUSTODY.md) — preregistration-stage custody;
- [`implementation/README.md`](implementation/README.md) — implementation/validation-stage record;
- [`implementation/STATUS.json`](implementation/STATUS.json) — validation-only terminal status with zero prospective executions;
- [`implementation/evidence/REMOTE_CUSTODY.md`](implementation/evidence/REMOTE_CUSTODY.md) — remote validation-only anchoring record.

Their `NOT_RUN` / zero-prospective language is historically correct for those stages. Current assay standing is given by this file, [`STATUS.json`](STATUS.json), [`../A2_V1_RESULT.md`](../A2_V1_RESULT.md), and [`EXECUTION_CUSTODY.md`](EXECUTION_CUSTODY.md).

## Current scientific frontier

A2-V1 closes the specific A2-V0 seam it preregistered: separation of cyclic encounter-order effects from active-survivor-priority effects within the tested family, including their interaction.

The mathematical substrate is recorded at [`../../formalization/GL_FIXED_SPACE_SEPARATION_V0.md`](../../formalization/GL_FIXED_SPACE_SEPARATION_V0.md). That record retains its historical theorem-earned / translation-unearned standing.

The subsequent [`../../formalization/GL_MATRIX_TRANSLATION_V0.md`](../../formalization/GL_MATRIX_TRANSLATION_V0.md) closes its finite executable translation obligation: an explicit nine-policy system has incomparable corrective frontiers despite maximal domain-restricted operator/trace agreement. It separately proves frontier washout under common inverse-decoder enrichment, refuting the policy-independent implication from fixed-space inequality to frontier inequality.

The construction explicitly assigns zero realization cost and uses a one-shot vector query; its resource contract differs from A2's coordinate queries. It establishes no temporal/learning result, informed inverse-selection procedure, human-control result, or general corrigibility claim. These are derived semantic results, not additional A2-V1 observations or empirical validation of MATRIX_0.

## Derived analysis: expansion without exact recovery

The post-execution [non-exact expansion record](analysis/NONEXACT_EXPANSION_V0.md) mines the sealed V0/V1 trajectories without rerunning either learner. It reproduces V0's 52 non-exact expansions and identifies 1,276 in V1's already-executed prospective stratum.

Of those 1,276, 580 imply strict expansion under every retained candidate operator; another 60 guarantee a particular gain while still permitting loss under a surviving candidate. The record proves a sufficient positive-feedback/complement condition for a shared two-state gain. These are conditional analyst-computed certificates; the learner does not compute or deploy them.

This analysis is **POST-EXECUTION / DERIVED / NON-PREREGISTERED**. It also distinguishes selecting the true hypothesis from a singleton survivor set. Its census, all 9,216 derived rows, standalone script and source bindings are linked from the record.

**No next assay is frozen by this record.** The next concrete missing argument must be chosen explicitly before any new experiment is designed. Neither GL record authorizes an affine assay or any other follow-up.

`MATRIX_1` remains **NOT EARNED**.
